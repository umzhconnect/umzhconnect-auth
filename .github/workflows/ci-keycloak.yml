name: CI - Keycloak image

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag to publish (defaults to short SHA)"
        required: false
        type: string
  push:
    branches: [main]
    paths:
      - "keycloak/**"
      - ".github/workflows/ci-keycloak.yml"
      - ".github/workflows/build-image.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    uses: ./.github/workflows/build-image.yml
    with:
      image: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}-keycloak
      context: keycloak
      target: prodimage
      tag: ${{ inputs.tag }}
