meta {
  name: AS JWKS (token signature keys)
  type: http
  seq: 2
}

get {
  url: {{keycloakUrl}}/realms/{{realm}}/protocol/openid-connect/certs
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.keys: isDefined
}
