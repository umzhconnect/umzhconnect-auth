# Node
node_modules/
dist/

# Terraform (state is written by the keycloak-config compose service)
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# Maven
target/

# OS
.DS_Store
dev.bru

# Corporate CA(s) dropped in for the provider-mirror build (env-specific)
tf-provider-mirror/certs/*.crt
tf-provider-mirror/certs/*.pem
